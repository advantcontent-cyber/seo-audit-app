# Supabase project (Settings > API)
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Google PageSpeed Insights API key (optional — without it, performance checks are skipped)
# Get one at https://console.cloud.google.com/apis/credentials after enabling the PageSpeed Insights API
PAGESPEED_API_KEY=
